import Vue from 'vue'
import Vuex from 'vuex'
import { carStore } from '../store/car-store.js'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    carStore
  }
})
